<html class="width">
<head>
    <title></title>
</head>

<button type="button" class="btn btn-default">Log In</button>
<button type="button" class="btn btn-default">Sign up</button>
<body class="background-home">
    <div class="search-box">
     <%= form_tag(games_path, :method => "get") do %>
     <%= search_field_tag :search, params[:search], placeholder: "Search Sport" %><%= search_field_tag :search, params[:search], placeholder: "Search Zipcode, Location" %>
          <%= submit_tag 'Search' %>
          <% end %>
    </div>

</body>
</html>
